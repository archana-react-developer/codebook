/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"],
  darkMode: "class", // Enable dark mode support
  theme: {
    extend: {
      colors: {
        dark: "#1E293B",
      },
    },
  },
  plugins: [],
};
